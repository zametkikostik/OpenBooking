# 🚀 OpenBooking - Quick Start Guide

## Локальный запуск

### 1. Запуск сервера разработки

```bash
cd /home/kostik/OpenBooking
npm run dev
```

Откройте **http://localhost:3000**

---

## 🔐 Админ панель

### Вход в систему

**URL:** http://localhost:3000/auth/signin

### Учетные данные администратора

```
Email: admin@openbooking.io
Пароль: Admin@123456
```

### Создание нового админа

Если нужно создать дополнительного админа:

```bash
node scripts/create-admin.js
```

---

## 📊 Доступные страницы

| Страница | URL | Описание |
|----------|-----|----------|
| **Главная** | http://localhost:3000 | Домашняя страница |
| **Вход** | http://localhost:3000/auth/signin | Страница входа |
| **Регистрация** | http://localhost:3000/auth/signup | Регистрация нового пользователя |
| **Dashboard** | http://localhost:3000/dashboard | Панель управления (после входа) |
| **Safe Vault** | http://localhost:3000/dashboard/vault | DeFi хранилище |
| **Legal Docs** | http://localhost:3000/legal/documents | Юридические документы |

---

## 🏗️ Архитектура

### Local Environment

```
┌─────────────────────────────────────┐
│   Browser: localhost:3000           │
│                                     │
│   ┌─────────────────────────────┐  │
│   │   Next.js Dev Server        │  │
│   │   (Port 3000)               │  │
│   └─────────────────────────────┘  │
│              │                      │
│              ▼                      │
│   ┌─────────────────────────────┐  │
│   │   Local Supabase            │  │
│   │   - PostgreSQL :54322       │  │
│   │   - Auth API :54321         │  │
│   │   - Studio :54323           │  │
│   └─────────────────────────────┘  │
└─────────────────────────────────────┘
```

### Ports

| Service | Port | URL |
|---------|------|-----|
| **Next.js** | 3000 | http://localhost:3000 |
| **Supabase API** | 54321 | http://localhost:54321 |
| **Supabase DB** | 54322 | postgresql://localhost:54322 |
| **Supabase Studio** | 54323 | http://localhost:54323 |

---

## 📝 Полезные команды

```bash
# Запуск dev сервера
npm run dev

# Сборка production
npm run build

# Запуск production
npm run start

# Проверка кода
npm run lint

# Локальный Supabase
supabase start
supabase status
supabase stop
```

---

## 🔧 Troubleshooting

### Ошибка "Port already in use"

```bash
# Найти процесс на порту 3000
lsof -i :3000

# Убить процесс
kill -9 <PID>
```

### Ошибка подключения к Supabase

```bash
# Проверить статус
supabase status

# Перезапустить
supabase stop
supabase start
```

### Ошибка "Module not found"

```bash
# Переустановить зависимости
rm -rf node_modules package-lock.json
npm install
```

---

## 📞 Support

Если возникли проблемы:

1. Проверьте `.env.local` - все ли переменные настроены
2. Убедитесь что Supabase запущен: `supabase status`
3. Проверьте логи: `npm run dev` выводит ошибки в консоль

---

**Enjoy building! 🎉**
